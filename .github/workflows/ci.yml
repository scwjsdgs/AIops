name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # 如果以后 Java 代码写完了，把这里的注释打开
  # java-build:
  #   name: Java Build & Test
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-java@v4
  #       with:
  #         java-version: '21'
  #         distribution: 'temurin'
  #     - run: cd ops_agent && mvn -B clean test

  # 如果以后 Python 代码写完了，把这里的注释打开
  # python-test:
  #   name: Python Test
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.12'
  #     - run: cd aiops-agent && pip install -r requirements.txt && pytest

  # 如果以后前端代码写完了，把这里的注释打开
  # frontend-build:
  #   name: Frontend Build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #     - run: cd ops-agent-front && npm install && npm run build

  # 当前仅验证仓库基础结构（保证 CI 能跑通）
  project-init-check:
    name: Project Initialization Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check project structure
        run: |
          echo "✅ 仓库结构检查通过"
          echo "当前包含模块："
          ls -d */ 